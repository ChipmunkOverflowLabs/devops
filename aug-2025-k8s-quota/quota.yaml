apiVersion: v1
kind: ResourceQuota
metadata:
  name: storage-limits-quota
  namespace: default
  labels:
    activeScope: "goldStorageLimits"
spec:
  hard:
    requests.storage: "50Gi"
    persistentvolumeclaims: "5"
    gold-storageclass.storage.k8s.io/requests.storage: "20Gi"
