apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: monitoring-stack
spec:
  selector:
    app: prometheus
  ports:
  - port: 9090
    targetPort: metricsPort
    name: prometheusPort
