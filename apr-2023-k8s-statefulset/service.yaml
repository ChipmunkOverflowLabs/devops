apiVersion: v1
kind: Service
metadata:
  name: db-headless-service
  labels:
    databaseType: "relationalDb"
spec:
  clusterIP: None
  ports:
  - port: 5432
    name: databasePort
  selector:
    app: postgres-db
